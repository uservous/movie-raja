steps:
  - command: "npm ci && npm version major && git push && git push --tags && npm run build && copy package*.json build && cd build && npm pack"
    artifact_paths:
    - "build/movie-raja*.tgz" 